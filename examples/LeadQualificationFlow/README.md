# Lead Qualification Flow — Example (AIFLOW v1.0.0)

This example demonstrates a lead scoring + qualification pipeline using AIFLOW.

Run:

```
npm install
npm run run-flow -- ./examples/LeadQualificationFlow/LeadQualificationFlow_v1.0.0.aiflow
```

Then open AIFLOW Studio → Debug Viewer to inspect execution.
